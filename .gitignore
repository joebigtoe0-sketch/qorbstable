
.anchor
.DS_Store
target
**/*.rs.bk
node_modules
.next
.env
.env.local
.data
.keys
test-ledger
.yarn

# TypeScript incremental build cache — Railway's builder mounts a cache dir at this path
*.tsbuildinfo

# Foundry build artifacts + local broadcast logs (addresses live in lib/evm/chains.ts)
evm/out
evm/cache
evm/broadcast
evm/lib
