import { AnalyticsClient } from "@/components/curve/AnalyticsClient";

export const metadata = {
  title: "Analytics — QORB",
};

export default function AnalyticsPage() {
  return <AnalyticsClient />;
}
